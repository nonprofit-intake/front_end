const INITIAL_STATE = {
    ailments: []
    ,
    isLoggedIn: false,
    isLoading: false
}

export const rootReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}